# 2026-08-02 · 캐시 제거 후 회귀 확인 + 부하 중 배치 실행 + 한계 부하 탐색

> **부하 테스트가 맞다** (게이트 ①): ①은 120 req/s에서의 구조 변화 회귀 확인, ②는 부하·배치
> 동시 발생 검증, ③은 한계 규모 식별(포화 관찰이 목적이므로 부하 외 방법이 없다).
> Artillery + 3단 지표 전부 사용.
> 아래 항목은 **측정 전 사용자 확정 완료 (측정 1·2: 2026-08-02, 측정 3: 2026-08-03).**
> 결과에 맞춰 수정하지 않는다.
>
> **선행 조건**: 캐시 제거 리팩터링 머지 (플랜: docs/superpowers/plans/2026-08-02-cache-removal.md,
> 커밋 `91743f0`~`413d8ba`). 비교 기준은 **2026-08-01 B 측정치**
> (docs/perf/2026-08-01-cache-vs-db-direct.md §7.1).

## 프로토콜 수정 (2026-08-03, 측정 착수 전 기록)

**사전 등록된 판정 기준(각 측정 §5)은 하나도 바꾸지 않았다.** 아래는 측정 형상·라운드 구성·
기준선 산출 방법의 변경이며, 결과를 보기 전에 적는다.

| # | 항목 | 변경 | 사유 |
|---|---|---|---|
| 1 | **측정 형상** | `2610218` → **`e6ac941`** (src는 `b52358f` 머지와 동일 — 커서 v3 + V28 포함) | 2026-08-03 사용자 결정. 브리프 작성 후 커서 v3(`prev_popular_score`·`place_stats_meta`)가 머지돼, 배포될 형상과 측정 형상을 일치시키려면 이쪽을 재야 한다 |
| 2 | **측정 3 래더** | 180 → 240 → 300 → (360) 에서 **360만 생략**. 실행은 180 → 240 → 300 | 360은 블로그 서술 범위 밖이고, 생성기 한계 구간에 들어가 "구조의 한계"와 "측정 환경의 한계"가 구분되지 않는다. 중단 조건은 그대로 — 성공률 <99% 라운드가 나오면 다음 단계 생략 |
| 3 | **측정 2 기준선** | "무부하 6.0초" 고정값 → **무부하 격자 1회를 새로 측정해 그 값을 게이트 기준으로 삼는다** | 커서 v3가 배치에 `prev_popular_score` 시프트 + meta 갱신을 추가했다. 6.0초는 그 변경 전 값이라 "2배 이내" 게이트의 분모로 쓰면 형상이 다른 값과 비교하게 된다 |
| 4 | **시나리오 경로 수정** | `place-list-popular-120.yml`의 `payload.path` `../` → `../../../` (커밋 `e6ac941`) | 2026-08-03 캠페인 정리 때 파일을 옮기며 생긴 전사 오류. 이 상태로는 Artillery가 payload 로드에서 ENOENT로 즉사해 측정 자체가 불가했다. 부하 파라미터는 손대지 않았다 |

### 2차 측정 (재실행) 프로토콜 수정 — 2026-08-03, 1차 결과 확인 후 사용자 승인

1차 측정에서 흠 2건이 드러나 **회귀(측정 1)와 한계(측정 3)를 재실행한다.** 측정 2(배치)는
4/4 충족이라 재실행하지 않는다. **사전 등록 게이트 수치는 그대로다** — 바꾼 것은 측정 방법이다.

| # | 항목 | 변경 | 근거 (1차 실측) |
|---|---|---|---|
| 5 | **`limit-processor.js`의 placeId 추출** | `1 + random()*6000` → **실제 활성 장소 id 6,320개(`place-ids.json`)에서 균등 추출** | 1차에서 POST의 **94.7%가 404**, 라운드 중 `Innodb_rows_updated = 0` — 증분 UPDATE가 한 번도 실행되지 않아 **쓰기 믹스가 재현되지 않았다.** 실제 `places.id`는 1~16000에 퍼져 있고 1~6000 구간엔 320개(5.1%)뿐이다. 균등성 전제("말할 수 없는 것" 7)는 유지 — "무엇에 대해 균등한가"만 실제 장소 집합으로 바로잡았다 |
| 6 | **워밍업 규약 1회 → 2회** | 라운드 전 워밍업을 2회 돌리고 둘 다 폐기 | 1차에서 p50이 8.9→7.9→7.0으로 3라운드 내내 단조 감소하고 버퍼풀 디스크 읽기가 25→12→3으로 **끝까지 0에 수렴하지 않았다**(08-01 B는 0). 워밍업 1회로는 정상 상태에 도달하지 못한다 |

**쓰기 유효성을 라운드마다 검증한다** — POST 2xx 비율과 `Innodb_rows_updated`·`Innodb_rows_inserted`
증가분을 라운드별로 기록하고, 쓰기가 다시 안 걸리면 다음 단계로 가지 않고 중단·조사한다.

1차 세트(`regression-r*`, `limit-r*`)는 **폐기하지 않고 보존**한다. 결과 문서에 두 세트를
병기하고, 판정은 2차 세트(`regression2-r*`, `limit2-r*`) 기준으로 갱신한다.

### 기록·해석 보강 (판정 불변)

- 라운드 표에 **mean을 p50 옆에 기록**하고, mean−p50 격차가 큰 라운드는 "꼬리 길어짐 신호"로
  한 줄 해석을 단다. **판정에는 쓰지 않는다.**
- **라운드별 artillery 실제 달성 rate를 기록**한다. 목표 rate 미달 라운드의 지연 수치는 다른
  부하를 잰 것이므로 그 사실을 명기한다.
- 측정 3의 포화 귀속에 **처리량 × 지연 교차 해석**을 쓴다. 단계가 올라가는데 달성 처리량이
  정체·하락하면서 지연이 오르면 적체(포화) 증거다. 처리량 곡선이 꺾인 단계를 표에 남긴다.
- **라운드 간 백분위를 평균 내지 않는다.** 대표값은 유효 라운드의 중앙값 라운드를 고르는 방식이고,
  백분위 재집계는 하지 않는다.
- p95는 "참고", 라운드 전체 p99·max는 "폐기" 딱지를 달아 기록만 한다 (지표 계약 A.3).

## 측정 1 — 캐시 제거 후 회귀 확인

### 1. 문제

08-01 판정은 "토글 위의 B"를 쟀다. 실제 배포될 코드는 다르다 — 캐시 철거, **커버링 인덱스(V26)**,
`ps.active` 제거(조인 가드 일원화), 북마크 검색 DB 전환, 배치 매시 30분. 판정이 실코드에서도
성립하는지 확인하지 않으면 "측정한 것과 배포하는 것이 다른" 상태가 된다.

### 2. 원인 가설

요청당 statements는 구조 변화가 없어 **~17.2 유지**. p50은 커버링 인덱스로 시 단위 쿼리
(트래픽 40%)가 6.46→0.806ms(단건 프로브 실측)로 줄어 **동급 이상**을 예상한다.

### 3. 기술적 선택

단일 경로 코드 + V26 위에서 08-01과 같은 시나리오·시드·부하로 재측정, 08-01 B 중앙값과 직접 비교.

### 4. 검증할 주장

**리팩터링이 성능 회귀를 만들지 않았다.** (개선은 덤이지 주장이 아니다)

### 5. 성공 조건 (사전 등록 — 2026-08-02 사용자 확정)

| 지표 | 기준 | 08-01 B 참조값 |
|---|---|---|
| 성공률 | 100% | 100% |
| 요청당 statements | **18.2 이하** (17.18 + 여유 1) | 17.18 |
| p50 | **7.2ms 이하** (6.0 × 1.2) | 6.0ms |
| DB CPU | 지속 80% 미만 | 최대 56.3% |

하나라도 어긋나면 **원인 지표로 귀속 조사 후 보고** (판정을 늘리지 말 것).
p99는 폐기 지표 — 판정에 쓰지 않되, 08-01 B에서 재현성(1.15배)이 확인됐으므로
**"참고" 딱지를 달아 기록만** 한다 (참조값 19.9ms).

## 측정 2 — 부하 중 배치 실행

### 1. 문제

배치가 매시 30분으로 바뀌어 피크 트래픽과 상시로 겹친다. 캐시 시절엔 배치 순간의 읽기가
캐시로 흡수됐지만, 지금은 **6,320행 UPSERT와 초당 수백 건의 정렬 읽기가 place_stats 한
테이블에서 만난다.** 08-01 문서 "말할 수 없는 것" 3번의 해소.

### 2. 원인 가설

무해할 것 — SELECT는 잠그지 않는 일관 읽기라 UPSERT 행 락과 대기 관계가 없고, 배치 창은
무부하 실측 ~6.0초다.

### 3. 기술적 선택

코드 변경 없이 **라운드를 xx:30에 걸치게 배치** — 120초 정속 구간 한가운데에 실제 스케줄
배치가 자연 발화하게 한다. 수동 SQL 실행은 앱 경로가 아니라서 기각 (스케줄러 → Facade →
Processor의 실제 트랜잭션 경계를 그대로 겪어야 한다).

> **방법 수정 (2026-08-03, 측정 전 사용자 지시).** xx:30 자연 발화 대기를 **벤치 한정 크론
> 오버라이드**(`BENCH_APP_JSON`으로 `solply.place-stats.cron`만 변경)로 대체한다. 수동 SQL
> 기각 사유는 "앱 경로 이탈"이었는데, 크론 오버라이드는 발화 시각만 바꾸고 스케줄러 →
> ShedLock → Facade → Processor 경로를 그대로 태우므로 그 사유에 걸리지 않는다. 대신
> "말할 수 없는 것"이 하나 늘어난다: 이 측정이 검증하는 것은 **"피크 부하 중 배치 1회
> 실행"의 영향**이지, 매시 30분이라는 발화 시각 자체가 아니다 (시각은 어차피 판정과 무관).

### 4. 검증할 주장

**매시 배치는 피크 부하 중에 돌아도 사용자 체감 영향이 없다.**

### 5. 성공 조건 (사전 등록 — 2026-08-02 사용자 확정)

| 지표 | 기준 |
|---|---|
| 배치 포함 라운드 성공률 | 100% |
| 배치 포함 라운드 p50 | 무배치 라운드 중앙값의 **1.2배 이내** |
| `Innodb_row_lock_waits` 증가분 | 무배치 라운드와 구분 불가 수준 |
| 배치 소요 (앱 로그 실측) | 무부하 6.0초의 **2배 이내** |

실패 시 → **새벽 고정 복귀** 또는 **UPSERT 청크 분할**을 숫자와 함께 들고 사용자 결정 요청.

## 측정 3 — 한계 부하 탐색 (2026-08-03 추가)

### 1. 문제

캐시 계층 없는 DB 직행 구조가 **어느 규모까지** 유효한지 실측 근거가 없다. 분산 캐시 기각(설계
문서 §3.2~3.4)은 현 규모 기준의 판단인데, 재검토 임계 규모가 숫자로 없으면 그 판단의 적용
한계가 공중에 뜬다.

### 2. 원인 가설

08-01 실측(120 req/s: 요청당 앱 CPU 4.4ms ≈ 앱 2대 용량의 13%, DB CPU 최대 56%, p50 6.0ms)을
선형 외삽하면 한계는 **240 req/s(목표의 2배) 이상**. 먼저 포화하는 자원은 **DB CPU**로 예상한다
(집계는 없어졌지만 정렬·조회가 전부 DB에 있다).

포화 자원의 정체가 곧 결론이다: **DB가 먼저면** "이 지점부터 리플리카/캐시 재검토"라는 조건이
서고, **앱이 먼저면** "읽기 저장소를 Redis로 바꿔도 한계 동일"이라는 더 강한 기각 근거가 선다.

### 3. 기술적 선택

캐시 계층 없는 `place_stats` 직조회 구조 유지 + **분산 캐시 재도입 조건을 부하 임계값으로 정의**.

### 4. 검증할 주장

현 구조가 **읽기·쓰기 혼합 부하**에서 목표의 2배(240 req/s)까지 성공률·지연 열화 없이 동작하고,
한계 지점에서 먼저 포화하는 자원이 무엇인지 식별된다. 쓰기를 섞는 이유(2026-08-03 사용자 지시):
분산 캐시와의 실질 차이는 **증분 UPDATE와 인기순 SELECT(조인)가 같은 place_stats에서 만난다**는
것이므로, 조회 전용 부하로는 이 주장을 검증할 수 없다.

### 5. 성공 조건 (사전 등록 — 2026-08-03 사용자 확정)

| 항목 | 기준 |
|---|---|
| 240 req/s 라운드 성공률 (5xx 기준) | **≥ 99.9%** — 5xx만 실패. 북마크 토글의 4xx는 도메인 충돌(이미 북마크됨 등)로 **별도 집계·비율 보고** |
| 240 req/s 라운드 **조회 계열 p50** (엔드포인트별 지표) | **≤ 18ms** (120 req/s 기준 6.0ms × 3). 토글 p50은 기록만 |
| `Innodb_row_lock_waits` | 라운드별 증가분 기록 — 락 대기 타임아웃(`1205`) 0건 |
| 한계 라운드(실패 발생 시) | 포화 자원이 지표로 특정될 것 — DB CPU ≥180% / 앱 CPU ≥180%(어느 한 대) / 커넥션 풀 대기 중 무엇인지 |
| 전 라운드 | 커서 중복·누락 0건 |

### 라운드·판정 규칙 (측정 1·2와 다른 부분)

- 단계: **180 → 240 → 300** req/s, 여유 있으면 360 추가. 시나리오는 이 캠페인
  `scenarios/place-list-popular-<rate>.yml` — **혼합 믹스**: 조회 90%(baseline 내부 비율
  36:18:18:18 유지) + 북마크 토글 10%(같은 placeId POST→DELETE, `limit-processor.js`)
- **측정 3은 반드시 측정 1 이후에 실행** — 토글이 시드를 소폭 변형한다 (성공 경로는 넷 제로,
  POST 4xx 후 DELETE만 시드 북마크를 실제 감소시킨다. 원본·통계가 함께 움직여 정합성은 유지)
- **유효성 규칙 조정**: 한계 탐색 라운드에는 "앱 CPU 200% 초과 = 무효" 규칙을 적용하지 않는다
  (포화가 관찰 대상이다). 단 **벤치 외 프로세스에 의한 호스트 오염은 여전히 무효**
- **중단 조건**: 성공률 <99% 라운드가 나오면 다음 단계는 돌지 않는다
- **측정 불가 판정**: artillery 생성기 자체가 호스트 CPU를 포화시키면 그 라운드는 구조의
  한계가 아니라 **측정 환경의 한계**로 기록한다 (라운드 중 artillery 프로세스 CPU 기록 필수)
- 커넥션 풀 대기로 한계가 나오면 **설정 한계이지 구조 한계가 아니다** — HikariCP 풀 크기를
  기록하고 구분해서 판정한다

## 이 측정으로 말할 수 없는 것 (미리 명시)

1. **LATEST 정렬의 부하 특성** — 시나리오가 인기순 중심 (08-01과 동일한 한계)
2. **북마크 검색의 부하 특성** — 시나리오에 없음. 근거로만 말한다: 페이징 없는 소량 조립이고
   IT가 기능 등가를 보장한다
3. **배치 무해성의 재현성** — 배치 포함 라운드는 1~2회다. "N회 재현"이 아니라
   "실측 1~2회 + 락 이론 근거"의 결합 주장임을 결과 문서에 명시. 경계선이면 라운드 추가
4. **로컬 절대치는 운영 SLO가 아니다** (지표 계약 A.3)
5. **한계 부하의 절대값은 이 벤치 사양(컨테이너 2vCPU)의 것이다** — 운영에서 유효한 것은
   "목표의 몇 배" 라는 비율과 "무엇이 먼저 포화하는가"라는 구조 특성뿐이다
6. **한계 탐색은 단계당 1라운드다** — 재현성 주장이 아니라 1회 관찰이다. 판정이 경계선에
   걸리면 해당 단계만 1회 반복한다
7. **쓰기 분산은 균등(placeId 1~6000 uniform)이다** — 핫 플레이스 집중 시의 증분 경합은
   이 측정이 아니라 2026-07-31 Task 8 실측(최악 집중 903 tx/s)이 말한다. 두 실측의 결합이
   전체 주장이다
8. **변화를 개별 변경에 귀속할 수 없다** (2026-08-03 추가) — 측정 형상 `e6ac941`에는 캐시 제거와
   **커서 v3(+V28)**가 함께 들어 있다. 따라서 08-01 B와의 차이를 "캐시 제거 때문" 또는 "커서 v3
   때문"으로 나눠 말할 수 없다. 이 비교는 **"배포 형상 vs 구형상"의 묶음 비교로만** 읽는다.
   애초에 검증할 주장이 "회귀 없음"이므로 귀속이 필요한 판정은 아니다

## 결과

**측정 실행 2026-08-03 16:45~17:41, 형상 `e6ac941`.**
결과 문서: [`docs/perf/2026-08-02-post-removal-regression.md`](../../../docs/perf/2026-08-02-post-removal-regression.md)

**판정 세트 = 4차 (`regression4-r*`, `limit2-r*`, 21:19~22:18, 깨끗한 호스트 load 3.84).**
1~3차는 보존·병기한다 (아래 "세트 이력").

| 측정 | 판정 | 핵심 수치 (판정 세트) |
|---|---|---|
| **1. 회귀 확인** | **부분 미충족 (3/4)** | 성공률 100% ✓ · statements **18.18** (≤18.2 ✓, 여유 0.02) · **p50 7.9ms (≤7.2ms ✗)** · DB CPU 39.5%/68.8% ✓. **3라운드 모두 p50 7.9ms로 재현** (디스크읽기 1/0/1 = 완전 워밍) |
| **2. 부하 중 배치** | **충족 (4/4)** | 성공률 100% · p50 **6.0ms** (무배치의 0.76배) · 락 대기 **+1건/4ms** · 배치 소요 **6.195s** (무부하 신규 기준선 5.72s의 1.08배) · **ShedLock 회차당 1인스턴스 실측** (재실행 안 함) |
| **3. 한계 탐색** | **부분 미충족** | 240: 조회 p50 **10.9ms** (≤18 ✓) · 락 타임아웃 **0** ✓ · **성공률 99.38% (≥99.9% ✗)**. 처리량 천장 **약 240 req/s**, 300에서 붕괴(달성 240/s 정체, p50 3,262ms, ETIMEDOUT 20,730) → **앱·DB CPU 동반 포화** |

### 판정 해설

- **측정 1의 p50 미달은 코드로 설명되지 않는다.** 커서 v3가 추가한 DB 작업은 `place_stats_meta`
  조회 1건 = **0.203ms**인데 미달 폭은 +1.9ms다. 1차에서 세운 "워밍 부족" 가설은 **판정 세트에서
  기각**됐다 — 디스크 읽기 0~1의 완전 워밍 상태에서 세 라운드 모두 7.9ms였다. 구조 지표
  (statements 18.18, `select_scan` 0.500, tmp_disk 0)는 08-01과 사실상 같다. 따라서
  **"회귀 없음"도 "회귀 있음"도 단정하지 않는다** — 구조는 그대로이고 p50은 이 환경에서 재현된다.
- **측정 3의 성공률 미달은 포화가 아니라 결함이다 (원인 귀속은 같은 날 정정 — 결과 문서 §7.4).**
  북마크 POST의 약 9%가 HTTP 500이고 그 비율이 180/240/300에서 **9.04%/7.89%/8.66%**로 부하와
  무관하게 일정하다. 실제 원인은 **북마크 중복 등록의 무가드**(유니크 제약 위반 1062 → 500,
  운영에서도 동일)다. 최초 귀속했던 증분 UPSERT 모호 컬럼 오류는 **벤치 DB에만 남은 실험 잔재
  컬럼**(`places.bookmark_count`, 07-29 비정규화 실험) 탓으로, 비동기 스레드에서 전 건 조용히
  실패했고 요청에는 전파되지 않았다. **1차의 프로세서 결함이 이 500들을 가리고 있었다.**
- **쓰기를 섞으면 병목 구조가 바뀐다.** 읽기 전용(1차) 300에서는 MySQL만 포화했으나(앱 58%),
  쓰기 믹스(4차) 300에서는 **앱·DB가 함께** 포화했다 (앱 136.5% avg·200%↑ 12샘플 /
  MySQL 147.6% avg·200%↑ 17샘플). 읽기를 Redis로 옮겨도 한계가 비례해 늘지 않는다는 뜻이다.
- **유효성 규칙의 한계를 발견했다.** 200% 초과 샘플 0개인 **유효** 라운드끼리도 호스트 부하에 따라
  p50이 6.0 / 8.9 / 7.9ms로 갈렸다. 규칙은 순간 스톨은 걸러내지만 **지속적 호스트 경합은 못 걸러낸다.**
  반면 `statements`는 12라운드 내내 **18.18로 완전 동일** — 지표 계약(A.3)의 근거가 재확인됐다.
- 1차 세트는 배치가 커서 v3에서 아직 돌지 않은 상태(세대 NULL)였고, 판정 세트는 배치 3회 이후라
  세대가 채워진 상태다. 정렬 축은 동일하나 **직전 세대 정렬 경로는 어느 세트에서도 미실행**이다.

### 세트 이력 (보존)

| 세트 | 라벨 | 호스트 load | 결과 | 왜 다시 쟀는가 |
|---|---|---:|---|---|
| 1차 | `regression-r1~r3`, `limit-r180/240/300` | 미기록 | 회귀 p50 중앙값 7.9ms(8.9/7.9/7.0) · 한계 240 통과, 300 붕괴(MySQL 단독 포화) | 워밍 부족(디스크읽기 25→12→3) + **쓰기 믹스 미재현**(`rows_updated`=0) |
| 2차 | `regression2-r1/r2/r2b/r3` | ~6.05 | 유효 2(p50 6.0 ×2) · **무효 2 연속** → 중단 | 호스트 오염 (네 컨테이너 동시 급등, 배정 상한 2배 초과) |
| 3차 | `regression3-r1·r2` | 6.6~7.6 | 유효 2 (p50 8.9 ×2) | 호스트가 시끄러워 판정 세트로 쓰지 않음 (§4.4) |
| **4차** | `regression4-r1~r3`, `limit2-r180/240/300` | **3.84** | **판정 세트** | — |

**무효 라운드 총계: 2건** (2차 `regression2-r2`·`r2b`, 같은 슬롯 연속 2회 → 규칙대로 측정 중단).
1·3·4차에는 무효 라운드가 없다. 측정 3의 180·240 라운드는 생성기 CPU가 등록 임계(≥90%)를 넘어
규칙대로 "측정 환경 한계" 딱지를 붙였으나, 달성 rate가 목표와 일치하고 300의 생성기 CPU는 오히려
더 낮았으므로(타임아웃 대기) 결론 방향은 흔들리지 않는다.

### 2차 세트 (재측정) — 호스트 오염으로 중단

흠 2건을 고쳐 재측정에 들어갔으나 **사전 등록 중단 규칙을 적용해 멈췄다.**

| 라운드 | 유효성 | p50 | statements | 디스크읽기 | 비고 |
|---|---|---:|---:|---:|---|
| warmup-a2-1 / -2 | 폐기 | 7.0 / 6.0 | 18.18 | **3,997 → 21** | 워밍업 2회 규약의 근거가 실측으로 확인됨 |
| regression2-r1 | **유효** | **6.0** | 18.18 | 6 | 200% 초과 샘플 0 |
| regression2-r2 | **무효** | 7.0 | 18.18 | 1 | 네 컨테이너 동시 급등 (app1 400.8% · app2 331.6% · mysql 436.3% · lb 56.7%), 달성 58/s |
| regression2-r2b | **무효** | 7.0 | 18.18 | 2 | 재실행도 app1 215.5% (상한 초과) → **연속 2회 무효** |
| regression2-r3 | **유효** | **6.0** | 18.18 | **0** | 완전 워밍 상태(08-01 B와 동일)에서 6.0ms |
| limit2-* | **미실행** | — | — | — | 회귀 단계에서 중단했으므로 착수 못 함 |

원인은 호스트다 — load average **6.05/7.10/6.10** (11코어), `mds_stores`(Spotlight 색인) 23.1%,
Orca 헬퍼 19%, Figma 6.3%, Notion 5.9%, ChatGPT 4.7%. 가이드의 측정 전 조건(재부팅 + IDE·브라우저·
AI 에이전트 종료)이 충족되지 않은 상태였다. **3번째 재시도는 하지 않았다** (무한 재시도 금지).

**유효 2라운드는 둘 다 p50 6.0ms = 08-01 B와 동일**하고, r3은 디스크 읽기 0(완전 워밍)에서 그
값을 냈다. 그러나 사전 등록 대표값은 **유효 3라운드의 중앙값**이므로 **판정으로 승격하지 않았다** —
따라서 위 판정표의 측정 1은 **1차 세트 기준 "부분 미충족" 그대로**다.

**프로세서 수정은 기능 검증 완료**: 실제 장소 id로 토글 1회 → POST 200 · DELETE 200,
`Innodb_rows_updated` **0 → 1** 증가 확인 (증분 UPDATE 경로가 실제로 타진다). **부하 상태 검증은 남음.**

### 무효 라운드 총계

- **1차 세트: 0건.** regression 3라운드 모두 앱 CPU 200% 초과 샘플 0개, 재실행 없음.
  측정 3의 180·240은 생성기 CPU가 등록 임계(≥90%)를 넘어 규칙대로 "측정 환경 한계" 딱지를
  붙였으나, 달성 rate가 목표와 일치하고 300의 포화가 MySQL CPU로 직접 관측되어 결론 방향은
  흔들리지 않는다 (결과 문서 §6.6).
- **2차 세트: 2건** (`regression2-r2`, `regression2-r2b` — 같은 슬롯 연속 2회) → 측정 중단.
