# load-test — 부하 테스트 디렉터리 지도

> **이 파일이 색인이다.** 어떤 산출물이 어느 측정에서 나왔는지, 무엇이 현행이고 무엇이
> 역할이 끝난 것인지 여기서 판별한다. 새 측정을 시작할 때도 여기부터 읽는다.
>
> **`load-test/`는 실행 가능한 것과 숫자의 원본**, **`docs/perf/`는 방법론과 해석**이다.
>
> - **왜·무엇을 재는가 (방법론)** → [`docs/perf/load-test-guide.md`](../docs/perf/load-test-guide.md)
> - **이 프로젝트 환경의 특성·지표 계약** → 같은 문서 §12
> - **개별 측정의 결과와 판단** → `docs/perf/<날짜>-<주제>.md`

---

## 1. 디렉터리 구조

```
load-test/
├── README.md                  ← 이 파일 (색인)
├── package.json               Artillery 등 의존성
├── data/                      users.csv — JWT 토큰. gitignore 됨 (절대 커밋 금지)
│
├── shared/                    캠페인 공통 도구 — 여러 측정에서 재사용
│   ├── sampler.sh             라운드 중 3초마다 서버 자원 시계열 → 캠페인 results/metrics/*.samples.csv
│   ├── metrics-snapshot.sh    라운드 전/후 MySQL 누적 카운터 diff → 캠페인 results/metrics/*.diff
│   │                          (둘 다 BENCH_CAMPAIGN 환경변수 필수 — 없으면 즉시 실패)
│   ├── generate-users-csv.mjs 부하용 JWT 발급 → data/users.csv
│   └── run-app.sh             [디버깅용] 호스트 bootRun (정식 측정은 컨테이너)
│
├── seed/                      벤치 DB 시드 생성기 모음 (캠페인 무관 공용 — 2026-08-03 통합)
│   ├── run-seed.sh · seed-bench-data.sql      기본 벤치 데이터 (유저 10만·북마크)
│   ├── generate-popular-seed.mjs · run-seed-popular.sh   Zipf 북마크 400만 추가
│   ├── generate-bench-seed.mjs · run-bench-seed.sh       전국 동네·장소 6,000 재시드
│   └── generate-review-seed.mjs · run-review-seed.sh     리뷰 시드
│
└── campaigns/                 ★ 측정 단위. 하나의 캠페인 = 하나의 증명하려던 주장
    └── <YYYY-MM-DD>_<주제>/
        ├── README.md          무엇을 증명하려 했는가 · 결과 · 근거 문서 링크
        ├── scenarios/         Artillery 시나리오
        ├── seed/              이 측정 전용 시드 생성기
        ├── bench/             이 측정 전용 스크립트·SQL (EXPLAIN, 상태 전환 등)
        └── results/           산출물 — 문서의 근거이므로 git으로 추적한다
            ├── reports/       Artillery JSON (aggregate + 10초 버킷)
            ├── metrics/       *.samples.csv · *.diff · *.start · *.end
            ├── explain/       EXPLAIN / EXPLAIN ANALYZE 출력
            └── query-logs/    p6spy 쿼리 로그 (쿼리 수를 세는 라운드에서만)
```

**핵심 규약: 종류별로 모으지 않고 측정별로 모은다.** 시나리오·시드·결과가 한 디렉터리에
있어야 "이건 어떤 측정의 산출물인가"가 경로만 보고 판별된다.

### 세 가지 계측 도구는 서로 다른 질문에 답한다

Artillery만 돌리면 **클라이언트가 체감한 결과**만 남는다. "p99가 900ms였다"는 알 수 있으나
*그 순간 서버 안에서 무슨 일이 있었는지*는 알 수 없다. 그래서 셋을 함께 쓴다.

| 도구 | 지표 종류 | 답하는 질문 | 시점 | 산출물 |
|---|---|---|---|---|
| `npx artillery` | **결과 지표** | 사용자가 무엇을 체감했나 (응답시간·성공률·처리량) | 라운드 내내 | `results/reports/*.json` |
| `shared/sampler.sh` | **자원 지표** | 어디가 한계였나 (앱·DB·edge CPU, 메모리, 커넥션) | 3초 간격 | `results/metrics/*.samples.csv` |
| `shared/metrics-snapshot.sh` | **원인 지표** | 왜 그렇게 됐나 (요청당 쿼리 수, 버퍼풀 hit rate, 디스크 읽기, filesort) | 라운드 전/후 1회 | `results/metrics/*.{start,end,diff}` |

이 구분은 [가이드 §5](../docs/perf/load-test-guide.md)의 3단 지표 구조와 일대일로 대응한다.
**세 층을 연결하지 못하면 개선의 원인을 증명할 수 없다** — "p95가 줄었다(결과)"는
"DB CPU가 줄었기 때문(자원)"이고 그것은 "요청당 쿼리를 줄였기 때문(원인)"이어야 한다.

샘플링이 실제로 결론을 가른 예 (baseline §2.3):

```
17:26:47   app1 19.29%   app2 15.75%   mysql 12.68%
17:26:52   app1 99.74%   app2 90.01%   mysql 56.51%   ← 세 컨테이너가 동시에 급등
17:26:57   app1 20.56%   app2 23.02%   mysql 15.64%
```

세 컨테이너가 같은 순간 튀었고 앱 CPU는 상한(200%)의 절반에서 멈췄다 → 서비스 병목이라면
나올 수 없는 조합이므로 **호스트 문제**로 판정했다. 샘플링이 없으면 "p99가 튀었다"에서 멈춘다.

---

## 2. 캠페인 색인

| 캠페인 | 증명하려는 주장 | 결과 | 상태 |
|---|---|---|---|
| [`2026-08-02_post-removal-regression`](campaigns/2026-08-02_post-removal-regression/) | ① 캐시 제거 후 실코드가 목표 부하(120 req/s)를 회귀 없이 처리하는가 ② 피크 중 배치 발화가 무해한가 ③ 읽기·쓰기 혼합에서 한계 규모와 먼저 포화하는 자원은 무엇인가 | ① 3/4 — p50 7.9ms(게이트 7.2) 3라운드 재현, +1.9ms는 코드 귀속 불가(meta 조회 0.203ms)·구조 지표 불변 ② 충족 4/4 (배치 1.08배·p50 0.76배·ShedLock 1인스턴스) ③ 쓰기 믹스 재현: 천장 ≈240 req/s(목표 2배), 300에서 **앱·DB CPU 동반 포화** → 읽기만 캐시로 옮겨도 한계 비례 증가 없음. 성공률 게이트 미달(99.38%)은 포화가 아니라 **북마크 중복 등록의 500 변환(무가드, 운영 동일)** 귀속 — 최초 귀속(증분 모호 컬럼)은 벤치 잔재 컬럼 탓으로 같은 날 정정(§7.4) → [결과 문서](../docs/perf/2026-08-02-post-removal-regression.md) | ⚠️ 부분 충족 + 운영 결함 1건·벤치 오염 1건 발견 (형상 `e6ac941`) |
| [`2026-08-04_read-ceiling`](campaigns/2026-08-04_read-ceiling/) | 읽기 전용 믹스에서 현 구조가 목표 피크(DAU 20만 산정 → 도착 300 req/s)를 받지 못하며 붕괴 병목은 앱이 아닌 DB CPU인가 — 통과 시 상한 탐색으로 분기 | **300 통과** (실처리 361 req/s · p50 6~7ms · 실패 0 · DB CPU 43%) → 등록 주장 **기각**, **직전 "300 붕괴"는 쓰기 10% 믹스 + 중복 북마크 500의 예외 로깅 몫으로 귀속 확정**. 상한 탐색 **500 붕괴** (시도 554 req/s에서 처리량 361→308~313 하락 + p50 1,500배 = 적체형) → **병목 = MySQL CPU**(cgroup 스로틀 68% vs 앱 10%대, I/O·정렬 폴백·락 전부 0). 생성기 아님(도착 결손 0, 2.6코어/11). 한계는 실처리 361 통과와 시도 554 붕괴 사이 — 협착 안 함. p95/p99 재현성 재실패(유효 라운드 간 3.7배), 1급 지표는 ±0.3% → [결과 문서](../docs/perf/2026-08-04-read-ceiling.md) | ✅ 목표 부하 통과 — 개선 캠페인 미개시(병목 부재). 미규명 1건(포화 시 요청당 행 읽기 2배) + 벤치 함정 2건 발견 (형상 `722f3b0`, src = `e6ac941`) |
| [`2026-08-04_saturation-amplification`](campaigns/2026-08-04_saturation-amplification/) | 위 캠페인의 미규명 관찰(포화에서 요청당 행 읽기 2배)이 인기순 쿼리의 **플랜 플립**(range→ALL) 때문인가 — 3조건(digest 실행당 증가 / 4시점 EXPLAIN 변경 / 인덱스 고정 시 소멸) 모두 충족해야 성립하도록 사전 등록 | **플랜 플립 기각** — ① 실행당 값 불변(커서 12,669→12,664 · 비커서 938→939 · `NO_INDEX_USED` 0) ② 4시점 플랜 완전 동일(ALL 미출현, 포화 중 EXPLAIN ANALYZE 1,800행·2.47ms). "2배 증폭"은 **분모가 만든 허상**(비싼 커서 문장 비중 16.6%→6.4%, 총 rows_examined는 오히려 122.1M < 146.5M). **등록에 없던 발견: 커서 쿼리(scroll p2)의 상시 나쁜 플랜** — 술어 전파(`ps.place_id > X` → `p.id > X`)로 주도 테이블이 places로 뒤집혀 실행당 12,663행(비커서의 13.5배), **부하 0에서도 동일**. FORCE INDEX로 1,822행 + 포화 처리량 155→**421 req/s**(2.7배, 실패 67.6%→27.0%) — 단 ③ **부분 성립**(붕괴 잔존) → **남은 천장은 플랜이 아니라 문장량 × 문장당 고정 CPU**(193~227us 불변 × 요청당 ~18문장) → [결과 문서](../docs/perf/2026-08-04-saturation-amplification.md) | ✅ 진단 완료 — 개선 표적 1호 확정(커서 플랜), 방식은 미결정. 교훈이 가이드 §5 "원인 지표의 분모"로 승격 (진단 도구: FORCE INDEX 토글·LB `worker_connections` 8192, 미커밋 형상) |

| [`2026-08-05_improved-recheck`](campaigns/2026-08-05_improved-recheck/) | 개선 형상(STRAIGHT_JOIN `82cec34` + SQL 18→11 `22358cb`·`2a50575`, role 클레임 토큰)이 목표 부하를 어떤 여유로 받고 이전 붕괴점(도착 500/s)이 어떻게 되는가 — 300 판정 3조건 + 500 통과 정의 + 해석 분기 4개 사전 등록 | **300 통과** (SQL/요청 **11.17** · `users` digest 소멸 · 커서 실행당 **1,822행** · MySQL 사용률 46.9→**28.6%**·스로틀 ~1% · 요청당 DB CPU **−39% = SQL 감소율과 산술 일치**) · **500 통과 — 분기 (a)** (시도 ≈600 req/s를 실패 0·도착률 100%로 처리, 기준선 155 대비 **3.9배**. p50 하위 조건만 2라운드 중 1 — 미충족 라운드는 호스트 경합 서명) · **650 관찰 = 붕괴, 병목 앱 이동** (앱 스로틀 52~59% vs MySQL 23%, LB 서킷브레이커가 붕괴 모양 증폭). 배치 cron **:30 확정** → [결과 문서](../docs/perf/2026-08-05-improved-recheck.md) | ✅ 목표 재확인 + 새 천장(시도 600~700)·다음 병목(앱 CPU) 확정 (형상 `82cec34`) |
| [`2026-08-06_place-stats-version-explain`](campaigns/2026-08-06_place-stats-version-explain/) | **부하 아님 — 실행계획 채취.** V29(버전 행)+V30(커버링 확장) 이후 인기순 조회가 `idx_place_stats_version_town_score` 하나로 서빙되는가. 사전 등록 예상 3개(단일 town range / 다중 town filesort / 조인 플립 없음) | ① **충족**(예상보다 좁은 `ref` — 동네 크기와 무관하게 **11행**에서 멈춘다) ② **충족**(정렬 입력 1,800 커버링 엔트리, 0.68~0.73ms) ③ **부분 충족** — 태그 없는 경로는 조인 자체가 없어 자명, 그러나 **태그가 붙으면 주도 테이블이 `place_tag`로 뒤집혀 정렬 인덱스를 잃는다**(`PRIMARY` eq_ref + weedout + filesort). 가장 비싼 조합에서 `place_tag` **10,536 엔트리 / 22.1ms** = 같은 시 단위 무태그의 **약 30배** → [결과 문서](../docs/perf/2026-08-06-place-stats-version-explain.md) | ✅ 채취 완료 — 태그 경로 플립 1건 발견, 개선은 미착수(V29/V30 귀속 여부는 비교 대상 부재로 미확정) |
| [`2026-08-06_place-list-latest-explain`](campaigns/2026-08-06_place-list-latest-explain/) | **부하 아님 — 실행계획 채취.** 정렬 축(`created_at`)이 인덱스에 없는 최신순이 어떤 비용을 치르는가. 사전 등록 예상 5개(항상 filesort / 커서 무의미 / LEFT JOIN이 정렬 전 / 버전 서브쿼리 상수 / 태그 플립은 덜 나쁨). 위 인기순 캠페인과 **같은 DB·같은 파라미터·1:1 대응 10케이스** | ① **충족**(전 케이스 filesort·커버링 아님, 동네 1개에서 **100행** 전부 — 인기순은 11) ② **충족**(커서 술어가 range 경계로 흡수 안 됨 → 1,800행 그대로 읽고 필터만 추가돼 1.72→**2.17ms**로 되레 느림) ③ **반증** — 아우터 조인 + 정렬 키가 전부 `p`에 있어 조인이 **정렬 뒤 11행에만** 붙는다(`loops=11`) ④ **충족**(`run only once`·`type=const`) ⑤ **부분 충족** — 플립 임계가 인기순과 **완전히 동일**, 배율은 13.8배로 작지만 **절대 시간은 23.8ms로 인기순(22.1ms)보다 비싸다**. 태그가 붙으면 두 정렬이 1.0~1.1배로 수렴 = 인기순 인덱스의 이점은 **무태그에서만** 발현 → [결과 문서](../docs/perf/2026-08-06-place-list-latest-explain.md) | ✅ 채취 완료 — 개선 후보 1건 확보(V11이 지운 `(town_id, active, created_at DESC)` 재검토), 태그 플립은 두 정렬 공통 부채로 확정 |
| [`2026-08-06_tag-filter-join-order`](campaigns/2026-08-06_tag-filter-join-order/) | **부하 아님 — 개선 전후 실행계획.** 태그 EXISTS의 `tags` 조인(=`t.active=1` 재검사)이 `filtered 10.00` 기본 추측을 낳아 태그 쪽을 **1,000배 과소평가**시키고 주도 테이블을 뒤집는가. 지우면 ① 흔한 태그에서 정상화 ② **희귀·0건 태그에서는 플랜 불변**(힌트와 갈리는 지점) ③ 결과 집합 불변 | ① **충족** — 동네 1개+태그 3개에서 주도 테이블이 지역으로 복귀, **66.5배**(인기순 11.30→0.17ms)·**67.4배**(최신순 12.40→0.18ms). 동네 18곳은 1,146<1,800이라 태그 주도가 유지되나 단건 조회(`loops` 합)가 23,946→1,631회로 줄어 **5.1·7.5배**(인기순 기준, 2026-08-10 정정 — 이전 색인의 "훑는 엔트리 10,536→1,146"은 오기) ② **충족** — 희귀 서브태그(185건)·결과 0건 태그는 **트리가 노드 단위로 동일**, 차이 10~20µs(노이즈) ③ **충족** — IT 32개 통과. 회차 2개(`baseline` = V31 이전 / `with-v31` = 근거). 태그 1개 조회는 **차이 없음** — PK 등호라 `tags`가 상수로 접혀 다른 태그 집합과 결합되지 않고, 10% 과소평가가 한 번만 적용돼 추정이 지역 후보보다 작아지지 않는다(before·after 트리 동일). V31 이전의 5.6배는 노이즈였고 정정. 힌트(`JOIN_PREFIX`)는 흔한 태그 4.2배 이득이나 **0건 태그에서 75배 손해**라 기각, 히스토그램은 같은 곳에 도착하나 `mysqldump` 미포함·자동 갱신 없음(조용한 회귀)이라 기각 → [결과 문서](../docs/perf/2026-08-06-tag-filter-join-order.md) · [블로그](../docs/blog/2026-08-06-tag-filter-join-order.md) | ✅ 개선 적용 — `appendTagFilters` 문자열 3개 (스키마·인덱스·힌트 변경 없음) |
| [`2026-08-06_latest-sort-index`](campaigns/2026-08-06_latest-sort-index/) | **부하 아님 — 인덱스 형태 판정.** 최신순 정렬 축(`created_at`)을 인덱스에 되돌릴 때 **ASC와 DESC 중 무엇인가.** V10이 만들고 V11이 지운 것은 `DESC` 형태였다. 사전 등록 4개(① ASC는 단일 town의 filesort 소멸 ② **DESC는 소멸하지 않는다** ③ 다중 town은 커버링만 얻는다 ④ 태그가 붙어도 ① 유지) | **4개 전부 충족.** ① ASC = `Backward index scan; Using index`, 0.149→**0.025ms** ② DESC = `Using index; Using filesort` 잔존, 0.062ms — **V10 형태를 되살렸다면 절반만 고쳤다** ③ 다중 town은 `Using index condition`→`Using index`, 2.030→**0.780ms**(ASC≈DESC) ④ 태그 조합에서도 단일 town은 정렬 없음. **DESC가 대조군이 되어 비용을 분해**: 시 단위 개선분은 **전부 행 복원 몫**, 동네 1곳은 행 복원 2/3 + 정렬 제거 1/3. 시 단위+태그 3개는 태그 주도라 **효과 없음**(3형상 동일) → [결과 문서](../docs/perf/2026-08-06-latest-sort-index.md) | ✅ 개선 적용 — `V31` (인덱스 교체) + `Place` 엔티티 `@Index`. IT 39개 통과 |

| [`2026-08-06_minimal-stability-final`](campaigns/2026-08-06_minimal-stability-final/) | 태그 필터 개선(`b24185a`)과 V31이 들어간 최종 형상이 목표 부하 120 req/s와 2.5배 300 req/s를 안정적으로 처리하는가 — 사전 등록 S1~S6 + 배치 겹침 C1~C5 | **120 통과** (도착률 100% · 실패 0 · 전체 p95 8.9ms · 앱 CPU 쿼터 24.1%·스로틀 0.7%) · **300은 오류 게이트만 통과** (도착률 100%·실패 0·락/디스크 정렬/커넥션 전부 0, 그러나 전체 p95 194.4ms로 지연 게이트 **미달**, 앱 스로틀 **18.2%**). 꼬리는 두 라운드 간 16~37배 산포로 **재현 실패** → 판정 근거로 쓰지 않는다. 요청당 statements **11.70**·태그 문장 행/실행 **659.7→659.4**로 부하 2.5배에서 불변 = 플랜 흔들림 없음, digest·EXPLAIN 모두 `tags` 조인 **부재** 확인. **배치 겹침 라운드(C) 통과** — 생성기 정지로 1회 폐기 후 cron 임시 override(`0 0/2 * * * *`)로 재시도, 정상 구간 t≈55s에 배치 **정확히 1회** 포착: C1~C5 전부 충족(배치 5,879ms vs 무부하 4,782ms = **+23%** · `current/prev` 한 세대 이동 · 2버전 **12,640행** · 조회 실패 **0**). 배치 비용은 **앱 CPU에만** 나타났고(쿼터 24.1%→31.9%, 스로틀 0.7%→2.6%) 지연은 A와 동등(정상 구간 p95 7.9ms 동일·p99 16.0→13.9ms). 사전 등록 §2의 두 가설은 **반증** — 버퍼풀 오염 없음(`Innodb_buffer_pool_reads` 0·hit rate 100%), 행 락 대기 0 **부록 D(사후 추가) — 앱 CPU 상한만 2→4 vCPU로 올린 대조 라운드 1회**: 다른 축(시나리오·호스트·MySQL 2 vCPU·제품 코드) 전부 고정, `docker update --cpus 4`로 재기동 없이 cgroup만 바꾸고 즉시 원복. 앱 스로틀 **18.2%/56.10s → 0.8%/0.58s**, 전체 **p95 194.4→13.9ms · p99 391.6→59.7ms**, **절대 평균 코어는 1.130→1.198로 줄지 않아** 쿼터 대비 %의 반토막이 분모 착시가 아님을 확인 → **앱 CPU 상한 병목 가설 지지**. DB 지표는 무변동(statements/요청 11.69·락 0·tmp disk 0·hit 100%). 곁다리로 **MySQL 스로틀 0.8→2.9% 증가가 관측**됐으나 B 두 라운드에서도 0.8/0.2%로 흔들린 값이라 단정하지 않고 **후속 확인 필요 항목**으로 남김. 단일 실행 + B의 꼬리 산포 16~37배 위의 관찰이라 **개선 배수는 주장하지 않는다** → [결과 문서](../docs/perf/2026-08-06-minimal-stability-final.md) | ✅ 완료 — 병목은 앱 CPU 쿼터로 확정(부록 D의 대조 실험으로 재확인), 배치 무해성은 **120 req/s에서만** 검증(300 겹침은 미측정) (형상 `93f0851`) |

| [`2026-08-06_app-saturation-diagnosis`](campaigns/2026-08-06_app-saturation-diagnosis/) | **부하 — 포화 징후 진단.** 직전 캠페인이 부하 두 점(120·300)·시작/종료 누계로만 봤던 "앱 CPU 병목"을 **계단 하나 안의 1초 해상도 시계열**로 분해한다. 도착 120→180→240→300→360/s 각 30초. 사전 등록 P1~P5 | **knee = S4→S5 확정** (도착 300→360 = 실처리 464→**554 req/s**에서 **p50 6→156ms, 26배**). **P2 성립** — S5에서 앱 절대 **1.898코어(쿼터 94.9%)** · **throttled period 66.0%**(1초 해상도로는 후반 **100% 지속**). **P1 불성립** — 처리량은 아직 도착률과 같은 배수로 늘었다. 개루프라 지연·적체가 먼저 터진 것이고, 버킷별 요청 vs 응답이 그 순간을 잡았다(+120건 적체 후 다음 버킷에서 배수). **= 붕괴가 아니라 붕괴의 시작점.** **P4 불성립(1/3)** — 앱 CPU pressure만 4.4→**29.6%**(6.65배)로 뛰고 context switch(29.8k→29.0k)·JVM live threads(**73 고정**, tomcat max 50)는 **S3에서 이미 포화**해 규칙의 "세 프록시 동반 증가" 가정이 깨졌다(규칙은 고치지 않고 사유만 기록). **P5 불성립(S4·S5)** — DB 절대 코어가 임계 1.0을 넘었으나(1.091·1.383) **비대칭은 그대로**: 앱 스로틀 66.0% vs MySQL **1.0%**, 디스크 읽기·락 대기·tmp disk **전부 0**, 버퍼풀 hit 100%. **가장 쓸모 있는 부산물: 요청당 앱 CPU가 부하 5.9배 구간에서 2.64~3.42ms로 거의 불변**(평균 3.05) → 천장이 알고리즘이 아니라 산술(2 vCPU ÷ 3.05ms ≈ **657 req/s**). 요청당 statements **11.68**·`tags` 조인 부재로 플랜 회귀 아님을 교차 확인. **Q5(프로파일)는 미측정** — JRE 이미지라 `jcmd`·`jdk.attach` 부재, BusyBox `nc` 유닉스 소켓 미지원으로 러닝 JVM에 JFR을 붙일 수 없다(원자료 `results/profile/jfr-capability.txt`). 지표로 추정해 채우지 않았다 → [결과 문서](../docs/perf/2026-08-06-app-saturation-diagnosis.md) | ✅ 진단 완료 — 단일 라운드라 **어떤 수치도 SLO로 승격하지 않는다**. 남은 표적: 평탄화 지점(도착 >360/s) 미관측, 3.05ms의 내역 미분해 (형상 `93f0851`, 제품 코드·compose 무변경) |
| [`2026-08-06_jfr-saturation-short`](campaigns/2026-08-06_jfr-saturation-short/) | 직전 캠페인의 미측정 Q5 — 300·360 arrivals/s에서 앱 CPU를 어떤 코드가 쓰는가. 기존 JRE를 기동 JFR로 녹화하고 앱·DB·요청 믹스는 유지 | **특정 태그/도메인 메서드 독점 없음.** Java CPU 표본의 최대 leaf 6.1% 이하, inclusive Hibernate **20.8~22.3%**, MySQL driver **12.7~13.0%**. 반복 신호는 실행 JAR URL/resource 처리(leaf 합 **9.8~13.2%**)와 Boot jar cache/internal monitor 경합(≥20ms monitor **155→261회**, 누적 11.5→18.4 thread-s). 상위 호출 스택은 5프레임에서 잘려 귀속 미완료. 표준 profile 라운드는 관측자 효과로 폐기했고 custom CPU JFC도 latency를 높여 **용량 수치에는 사용 금지**, hotspot 방향만 채택 → [결과 문서](../docs/perf/2026-08-06-jfr-saturation-short.md) | ✅ 짧은 프로파일 완료 — 앱 2 vCPU·원래 JVM 옵션·HTTP 200 복구 확인, 제품 코드 무변경 |

| [`2026-08-07_place-skeleton-cache`](campaigns/2026-08-07_place-skeleton-cache/) | 장소 단위 골격 스냅샷 캐시(불변 5필드, `volatile Map`, 매시 카운트 배치 직후 통째 교체)가 **요청당 앱 CPU를 깎아** 목표 부하(300 arrivals/s)의 스로틀을 낮추고 knee를 360 위로 미는가. 계단을 **480까지 확장**해 knee가 옮겨간 위치를 볼 수 있게 함 | **채택 — 3조건 전부 충족.** 정합성 게이트 통과(5요청 응답 body·커서 토큰 동일), 목표 부하 스로틀 **29.0% → 2.6%**, knee가 **사다리(480)를 벗어남**. 요청당 앱 CPU **3.206 → 1.817ms(−43.3%)**, 앱 코어 1.501 → 0.848. OFF는 S6(420)부터 붕괴(스로틀 100% 3/3 재현·p50 1,790ms·`ETIMEDOUT` 3,397~9,189건), ON은 S7(480 = **744.6 req/s**)까지 **p50 5ms·실패 0**. 원인: `places`·`place_images` digest 실행 **96,8xx회 → 0회**(미스 0·히트율 100%)로 SELECT 6→4. DB 부작용 없음(코어 1.047→0.680·락 0·디스크 읽기 0). 스냅샷 빌드 **6,320개 302ms(콜드)/94ms(웜)**. **사전 예상 10~15%가 43.3%로 크게 빗나갔고**, "프로파일 표본 비율로 절감량을 예측하면 안 된다"를 교훈으로 기록. 부분 포화 구간 스로틀은 라운드 간 2~3배 흔들려(앱 코어는 ±4.5%) **단독 판정 근거로 약함**을 함께 기록 → [결과 문서](../docs/perf/2026-08-07-place-skeleton-cache.md) | ✅ 채택 — **ON의 천장은 미확정**(480에서 아직 안 무너짐). 앱 코어 2.002~2.005 문면 위반은 포화 계단의 1초 표본 읽기 오차로 사유 기록 후 존치 (형상 `3dfbeb4`) |

| [`2026-08-09_projection-vs-snapshot`](campaigns/2026-08-09_projection-vs-snapshot/) | 골격 절감의 **분해** — 채택 문서의 대안 표에 없던 DTO 직접 프로젝션을 같은 빌드 세 방식(entity/projection/snapshot, 요청당 골격 쿼리 2·2·0)으로 사후 검증. projection은 스냅샷 로더의 SQL·조립식을 페이지 id로만 돌려 **e−p = 하이드레이션 몫, p−s = 쿼리 왕복 몫**이 되게 설계. 방식 교대(E→P→S × 3사이클) + 라운드마다 fresh JVM으로 직전 캠페인의 라운드 순서 효과 처방을 반영, size=10(운영 기본) | **갈래 (b) — 앱 CPU에서 snapshot ≈ projection.** 정합성 게이트 통과(세 방식 응답·커서 5/5 동일, 전환은 digest alias로 검증). 요청당 앱 CPU 중앙값 **E 2.403 / P 1.918 / S 1.876ms**이나 호스트 상시 부하로 산포 11~22%(직전 ±4.5%)라 **앱 CPU 단독으로는 두 몫 다 "구분 불가"** — 원인 지표가 가른다: **요청당 힙 E 1,005 / P 761 / S 696KB(산포 2.0%)** = 하이드레이션 몫 −244KB, 쿼리 제거 몫은 statements **−2.00/요청**·DB CPU **−0.282ms(−17.1%, 산포 밖)**·커넥션 점유 **−0.48ms**. 420 관찰(E·P만, 협의해 축소): projection 스로틀 **3.5%** vs entity **58.7%**, 둘 다 실패 0 — **entity 붕괴 미재현**(직전 3/3과 워밍업 진입 경로가 다름, 미규명이라 반증으로 안 씀). 부산물: **콜드 JVM의 entity 요청당 CPU 10.4ms**(웜 2.4ms의 4.3배) — 고정 120 arrivals 워밍업이 그 자체로 붕괴해 사다리 워밍업으로 교체(폐기 4건 전부 계측·워밍업 결함, 결과를 보고 버린 라운드 0) → [결과 문서](../docs/perf/2026-08-09-projection-vs-snapshot.md) | ✅ 분해 완료 — 스냅샷 유지, 근거의 무게중심이 앱 CPU에서 "쿼리 제거(DB·커넥션) + 요청과 무관한 갱신"으로 이동 (형상 `f91081c`) |

| [`2026-08-10_place-tag-covering-index`](campaigns/2026-08-10_place-tag-covering-index/) | **부하 아님 — 인덱스 형태 판정.** `place_tag`의 태그 방향 인덱스가 `(tag_id)` 단일 컬럼이고 PK가 대리키 `id`라 리프에 `place_id`가 없다 — 태그로 장소를 찾는 유일한 용도인데 커버링이 아니다. `(tag_id, place_id)`로 바꾸면 ① 인기순 서울+태그3개의 `uk` 전체 스캔(15,461)이 사라지고 ② 최신순의 숨은 클러스터드 조회가 사라지며 ③ **어떤 조합도 나빠지지 않는가** | **기각.** ① **평가 불가 — 전제가 이미 없다.** V32(place_stats 단일행·인덱스에서 version 제거)가 인기순의 태그 접근 경로까지 바꿔 놓아, 손대기 전 before가 이미 `Index lookup (tag_id=1) rows=1,146`이다 ② **충족** — `pt` 노드가 커버링으로 바뀌어 0.717→**0.157ms(4.6배)**, 최신순도 0.774→0.179ms. 시 단위 8칸 전부 **1.2~1.5배**(주도 테이블 불변) ③ **미충족** — 최신순·동네 1곳·희귀 서브태그(185건)에서 **주도 테이블이 지역→태그로 뒤집히고 정렬 노드가 새로 생겨** 0.172→**0.352ms(2배)**. 옵티마이저는 after를 더 싸다고 봤다(비용 115→91.2) — 역방향 인덱스 스캔이 정렬을 공짜로 주는 이점이 비용 모델에 안 잡힌다. **힌트를 기각했던 것과 같은 모양**(특정 입력만 개선)이고 크기만 75배 vs 2배로 다르다. ④ **충족** — IT 37개 통과 → [결과 문서](../docs/perf/2026-08-10-place-tag-covering-index.md) | ⚠️ **등록 기준 기각 → 사후 채택**(결과 문서 §9). 인덱스 형태의 정합성을 우선하고 손해(절대 +0.18ms·한 조합)를 감수하는 판단이며, 되돌리기가 마이그레이션 한 줄이라는 점이 근거다. `V33` + `PlaceTag` `@Index`. 남은 표적: 뒤집힘 임계 탐색, `idx_place_tag_place_id` 중복 제거(묶지 않았다) |

> **과거 캠페인 정리 (2026-08-03).** 2026-07-13 ~ 2026-08-01의 캠페인 6개는 작업 디렉터리에서
> 제거했다 — 현재 서사에 필요한 검증은 위 캠페인이 담당하고, 과거 산출물의 원자료·판정은
> git 히스토리와 `docs/perf/`의 결과 문서에 남아 있다. 되살리려면 히스토리에서 해당 경로를
> checkout하면 된다.

정리되어 남지 않은 측정:

| 측정 | 산출물 | 비고 |
|---|---|---|
| `#349`·`#350`·`#352` 북마크 API 최적화 (2026-03) | **없음 (삭제)** | 환경·데이터가 현행과 달라 재현·비교 불가. 수치는 각 PR과 커밋 이력에 남아 있다 |
| `#381` 캐시 벤치 (2026-07-13) | **없음 (폐기)** | Artillery `environments` 병합 버그로 의도(55초)와 다르게 실행됨. 폐기 사유는 `docs/perf/2026-07-30-place-popular-baseline.md` §0 |

---

## 3. 현행 측정 실행법

### 토폴로지 (2026-08-05 변경)

`docker-compose.bench.yml`은 **앱 1대(`solply-bench-app`) + edge(`solply-bench-lb`) + MySQL + Redis**다.
직전까지는 앱 2대 + nginx 부하 분산이었고, 가용성 요구를 철회하면서 인스턴스를 하나로 줄였다
(결제 없는 서비스라 잠깐의 중단 수용, DB까지 이중화해야 완결되는 논리라 반쪽 HA는 접는다).
nginx는 남는다 — **부하 분산이 아니라 운영 경로 재현용 edge**이고, 프로세스 수와 edge는 다른 축이다.
진입점은 그대로 `localhost:8082`라 시나리오 파일의 target은 유효하다. upstream이 하나이므로
`max_fails=0`으로 서킷브레이커를 껐다(차단이 곧 전면 장애이고, 지난 캠페인에서 포화 모양을 왜곡했다).

> ⚠️ **이후 측정은 단일 프로세스 재기준선이다.** 기존 캠페인(앱 2대 + LB)의 처리량·레이턴시와
> **절대값을 비교하지 말 것** — 비교하려면 이 형상에서 기준선을 다시 잡아야 한다.
> `sampler.sh`의 CSV 컬럼도 `app1/app2` 두 칸에서 `app` 한 칸으로 바뀌었다.

기동·검증 절차 전체는 [`docs/perf/load-test-guide.md`](../docs/perf/load-test-guide.md) §12에 있다.
여기서는 경로 규약만 정리한다.

```bash
cd load-test

# 1) 대상 캠페인을 한 번 export한다 — sampler와 metrics-snapshot이 같은 곳에 기록한다
export BENCH_CAMPAIGN=2026-07-30_place-popular-baseline

# 2) 토큰 (401이면 재발급)
node shared/generate-users-csv.mjs

# 3) 라운드
./shared/metrics-snapshot.sh start baseline-r1
./shared/sampler.sh baseline-r1 200 &
npx artillery run campaigns/$BENCH_CAMPAIGN/scenarios/place-list-popular.yml \
  -o campaigns/$BENCH_CAMPAIGN/results/reports/baseline-r1.json
wait
./shared/metrics-snapshot.sh report baseline-r1
```

`BENCH_CAMPAIGN`이 없거나 존재하지 않는 캠페인이면 두 도구가 **즉시 실패**하고 사용 가능한
캠페인 목록을 출력한다 — 산출물이 엉뚱한 곳에 쌓이는 것을 막기 위함이다.

---

## 4. 새 캠페인을 시작할 때

1. **먼저 [`docs/perf/load-test-guide.md`](../docs/perf/load-test-guide.md) §3의 다섯 항목을 작성한다** —
   문제 / 원인 가설 / 기술적 선택 / 검증할 주장 / 성공 조건. 이걸 건너뛰면 측정해도 결론이 안 난다.
2. 디렉터리를 만든다.
   ```bash
   C=campaigns/$(date +%Y-%m-%d)_<주제>
   mkdir -p $C/{scenarios,seed,bench,results/{reports,metrics}}
   ```
3. `$C/README.md`에 1번에서 정한 내용을 적는다. **측정 전에 적는다** — 사후에 쓰면 결과에 맞춰
   주장을 조정하게 된다.
4. 시나리오는 기존 것을 복사하지 말고 [`place-list-popular.yml`](campaigns/2026-07-30_place-popular-baseline/scenarios/place-list-popular.yml)을
   본으로 삼는다. `phases`를 단독 정의하고 **`environments`를 쓰지 않는다** (§5).
5. 시나리오의 `payload.path`는 캠페인 깊이 기준 `../../../data/users.csv`다.
6. `.gitignore`는 고칠 필요가 없다 — `campaigns/**/results/`는 이미 추적 대상이다.
7. 측정이 끝나면 `docs/perf/<날짜>-<주제>.md`에 결과를 쓰고, 이 파일 §2 색인에 한 줄 추가한다.

---

## 5. 알려진 함정

과거에 실제로 겪은 것만 적는다. 새 측정 전에 훑는다.

| 함정 | 증상 | 대응 |
|---|---|---|
| **Artillery `environments`** | phase 배열이 기본 `phases`와 **원소 단위로 병합**돼 기본 프로파일이 잔존. `#381`이 의도 55초 → 실제 175초로 실행되어 폐기 | `phases`를 단독 정의한다. `environments`를 쓰지 않는다 |
| **p6spy 쿼리 로깅** | 초당 240쿼리를 전문 로깅해 앱 CPU가 포화. PK 단건 조회가 495ms까지 늘어 레이턴시 측정이 무의미해짐 | `LOGGING_LEVEL_P6SPY=OFF` (compose에 반영됨). 쿼리 **수**를 셀 때만 별도 라운드로 켜고, 그 라운드의 레이턴시는 비교하지 않는다 |
| **환경변수와 `docker restart`** | 변경한 환경변수가 반영되지 않는다 | `up -d --force-recreate` 로 컨테이너를 재생성한다 |
| **`--remove-orphans`** | `docker/` 안 compose 5개가 프로젝트명을 공유해 무관한 컨테이너까지 삭제 (실제 사고) | 이 플래그를 쓰지 않는다 |
| **nginx upstream IP 캐시** | 앱만 재기동하면 nginx가 stale IP를 붙들고 전 요청 502 | 컨테이너에 고정 IP 부여 (compose에 반영됨) |
| **호스트 자원 경합** | 앱과 MySQL이 **동시에** 수 초간 멈춤. p99가 16배 흔들림 | 측정 전 재부팅 + IDE·브라우저·AI 에이전트 종료. 오염된 라운드는 **구간을 잘라내지 말고 라운드 전체를 폐기**하고 재실행 |
| **첫 라운드 JIT** | r1만 유별나게 느리고 이후 단조 회복 | 첫 라운드는 워밍업으로 간주하고 버린다 |
| **`payload.path` 상대경로** | 토큰 CSV를 못 찾아 전 요청 401 | 캠페인 깊이 기준 `../../../data/users.csv` |
| **전역 카운터 ÷ 요청 수로 원인 판정** | 요청 믹스가 부하에 따라 변해 "요청당 평균"이 허상을 만든다. 실측: 실행당 13.5배 비싼 커서 문장의 비중이 포화에서 **16.6%→6.4%**로 변한 것을 "요청당 행 읽기 2배 증폭"으로 오독하고 플랜 플립 가설까지 세웠다(총 rows_examined는 오히려 감소했다) | 원인 판정은 **`performance_schema` digest의 실행당 지표**(`ROWS_EXAMINED ÷ COUNT_STAR`) + **동일 파라미터·동일 세션 EXPLAIN의 시점 비교**로 한다. 분모에 클라이언트 관점 수치(성공 응답 수)를 쓰지 않는다 — 클라이언트가 포기해도 서버는 일한다 (가이드 §5 "원인 지표의 분모", 2026-08-04 saturation-amplification) |
| **docker stats CPU 과대 보고** | 고동시성 라운드에서 배정 상한을 넘는 값이 찍힌다 (실측: 상한 200%인데 MySQL **320.8%**·앱 263.7%). 유효성 규칙("앱 CPU 상한 초과 → 폐기")을 문면대로 적용하면 정상 라운드가 무효가 된다 | cgroup이 정본이다 — `cpu.max`(쿼터 강제)와 `cpu.stat`(`usage_usec`·`nr_periods`·`nr_throttled`)로 확인한다. **유효성 판정에 docker stats 문면을 쓰지 않는다** (2026-08-04 read-ceiling §9.1) |
| **nginx `worker_connections` 기본 1024** | 실처리 ~550 req/s에서 커넥션이 고갈돼 **LB가 500을 낸다**. 앱 예외는 0건이라 앱 로그만 보면 원인 불명 (실측: 500 전량 160건이 LB 에러 로그 건수와 정확히 일치) | 고부하 측정 전에 `worker_connections`를 상향한다(361 req/s 초과 측정의 선행 조건). **앱 예외 0인데 5xx가 보이면 LB 에러 로그를 먼저 본다** (2026-08-04 read-ceiling §9.2) |
| **계측 오버헤드** (미해결) | `sampler.sh`가 3초마다 `docker exec mysql` 2회 → DB 컨테이너 CPU를 계측이 소비 | 인지된 한계. 경량화 예정 |
| **부하생성기 위치** (미해결) | Artillery가 측정 대상과 같은 호스트에서 동작 | 인지된 한계. 1급 지표로 판정하는 한 치명적이지 않다 |

---

## 6. 캠페인 간 의존성

원칙은 캠페인의 독립이지만, 시드에 하나 남아 있다.

```
2026-07-29_place-popular-sort/seed/run-seed-popular.sh
  └─(bench 유저 10만이 없으면 선행 실행)→ 2026-07-13_bookmark-index/seed/run-seed.sh
```

`2026-07-30_place-popular-baseline/seed/run-bench-seed.sh`는 **독립적이다** — 유저·장소·북마크를
전부 스스로 생성하므로 다른 캠페인의 시드가 필요하지 않다. 현행 측정은 이것을 쓴다.
